# Docker Foundation

`compose.yaml` at the repository root starts PostgreSQL and the Fastify backend foundation. The compose credentials are local-development defaults only. Production deployment must inject secrets and use the procedures defined in `docs/SECURITY.md` and `docs/adr/ADR-009-deployment-and-hosting-strategy.md`.
