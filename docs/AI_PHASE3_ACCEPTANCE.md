# Phase 3 acceptance matrix

## Phase 3-A embedding architecture and lifecycle

| Requirement                                                                    | Implementation                                                                 | Test                                                                                                                       | Status                   |
| ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| Device-scoped ownership and derived-data synchronization decision              | `AI_EMBEDDINGS.md`, `AI_ARCHITECTURE.md`, namespace owner columns              | `embedding-namespace-service.test.ts` safe deletion and migration tests                                                    | Implemented and verified |
| SQLite vector storage decision and packaging fallback                          | encrypted SQLite `ai_vector_*` JSON-vector tables, no external vector database | `embedding-namespace-service.test.ts` fresh/upgraded initialization                                                        | Implemented and verified |
| Stable chunking policy, deterministic chunk IDs and hashes                     | `electron/ai/embedding-chunking.ts`                                            | `embedding-namespace-service.test.ts` (`creates deterministic chunk IDs and hashes`)                                       | Implemented and verified |
| Versioned namespace schema                                                     | migration 17 `ai_vector_namespaces`                                            | `embedding-namespace-service.test.ts` fresh/upgraded migration initialization                                              | Implemented and verified |
| Chunk schema and uniqueness                                                    | migration 17 `ai_vector_chunks`, `ai_vector_chunks_source_unique_idx`          | `embedding-namespace-service.test.ts` chunk upsert coverage                                                                | Implemented and verified |
| Vector schema and compatibility metadata                                       | migration 17 `ai_vector_records`                                               | `embedding-namespace-service.test.ts` dimension mismatch rejection                                                         | Implemented and verified |
| Namespace events and bounded diagnostics                                       | migration 17 `ai_vector_namespace_events`                                      | `embedding-namespace-service.test.ts` lifecycle transitions                                                                | Implemented and verified |
| Create, inspect, rebuild/resume, verify, activate, deprecate, delete lifecycle | `electron/ai/embedding-namespace-service.ts`                                   | `embedding-namespace-service.test.ts` lifecycle cases                                                                      | Implemented and verified |
| Old active namespace remains active during incomplete rebuild                  | atomic activation checks in `EmbeddingNamespaceService.activate`               | `embedding-namespace-service.test.ts` (`keeps the old active namespace during incomplete rebuild`)                         | Implemented and verified |
| Coverage validation followed by atomic activation                              | `verifyCoverage` and activation transaction                                    | `embedding-namespace-service.test.ts` (`valid coverage activates atomically`)                                              | Implemented and verified |
| Namespace deletion preserves canonical logs                                    | derived-only delete in `EmbeddingNamespaceService.delete`                      | `embedding-namespace-service.test.ts` (`deletes only derived namespace data and preserves canonical logs`)                 | Implemented and verified |
| Local-mode and cloud-consent enforcement                                       | `EmbeddingNamespaceService.create`, provider descriptors, AI settings/consent  | `embedding-namespace-service.test.ts` (`enforces Local mode and cloud consent`)                                            | Implemented and verified |
| Safe read-model redaction                                                      | `SafeEmbeddingNamespace` projection                                            | `embedding-namespace-service.test.ts` (`returns safe projections without vectors, text, credentials, or ownership tokens`) | Implemented and verified |

Phase 3-A verification commands: `pnpm --filter @focuslog/desktop exec vitest run --config vitest.config.ts electron/ai/embedding-namespace-service.test.ts`, `pnpm --filter @focuslog/desktop lint`, `pnpm --filter @focuslog/desktop typecheck`.

## Phase 3-C facts and knowledge graph

| Requirement                                                                                             | Implementation                                                                                                                                  | Test                                                                                                                                                                                    | Status                   |
| ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| Versioned evidence-backed fact schema and status history                                                | migration 19 `ai_fact_records`, `ai_fact_record_evidence`, `ai_fact_status_history`, `ai_fact_corrections`; `electron/ai/fact-graph-service.ts` | `fact-graph-service.test.ts` evidence/lifecycle tests                                                                                                                                   | Implemented and verified |
| Automated extraction requires valid evidence and rejects unsupported output                             | `FactGraphService.extractFactsFromSource`, `validateEvidence`, predicate/type validation                                                        | `requires valid evidence, rejects unsupported output, and redacts safe diagnostics`                                                                                                     | Implemented and verified |
| Reinforcement, supersession, contradiction, temporal validity, manual rejection, and correction overlay | `FactGraphService.reconcileSubject`, `rejectFact`, `correctFact`                                                                                | `handles reinforcement, contradiction, temporal facts, rejection, correction overlay, and stale propagation`                                                                            | Implemented and verified |
| Source deletion/staleness propagation                                                                   | `FactGraphService.markSourceStale`, unsupported relation updates                                                                                | `handles reinforcement, contradiction, temporal facts, rejection, correction overlay, and stale propagation`; `keeps owner isolation and excludes deleted evidence from active support` | Implemented and verified |
| Persistent graph nodes, aliases, relations, evidence, audit events, merge/split safeguards              | migration 19 graph tables; `updateGraphFromFact`, `createRelation`, `resolveEntity`, `splitEntity`                                              | `creates evidence-backed graph records, stores conservative aliases, splits safely, and rejects alias cycles`                                                                           | Implemented and verified |
| Queue jobs for fact/graph extraction, reconciliation, stale marking, rebuilds, and cleanup              | `AIJobKind` additions; `FactGraphService.queueHandlers`                                                                                         | `runs fact and graph jobs through the durable worker without duplicate results`                                                                                                         | Implemented and verified |
| Owner isolation and secret-free diagnostics/projections                                                 | owner predicates in service reads/writes; redaction helpers                                                                                     | `requires valid evidence, rejects unsupported output, and redacts safe diagnostics`; `keeps owner isolation and excludes deleted evidence from active support`                          | Implemented and verified |

Phase 3-C verification commands: `pnpm --filter @focuslog/desktop exec vitest run --config vitest.config.ts electron/ai/fact-graph-service.test.ts electron/database/ai-migration-compat.test.ts`, `pnpm --filter @focuslog/desktop lint`, `pnpm --filter @focuslog/desktop typecheck`.

## Phase 3-D hierarchical memory and retrieval

| Requirement                                                                                           | Implementation                                                                                                     | Test                                                                                                       | Status                   |
| ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- | ------------------------ |
| Unified retrieval/staleness provenance tables                                                         | migration 20 `ai_memory_staleness_events`, `ai_retrieval_plans`, `ai_retrieval_plan_items`, `ai_memory_qa_answers` | `memory-retrieval-service.test.ts`; migration compatibility suite                                          | Implemented and verified |
| Source edit/delete propagates staleness to affected summaries, chunks/vectors, facts, and graph only  | `MemoryRetrievalService.propagateSourceChange`                                                                     | `propagates source staleness exactly across hierarchy, vectors, facts, and graph without unrelated owners` | Implemented and verified |
| Parent cannot appear current with stale child                                                         | recursive `markAnalysesStale` through `ai_analysis_child_sources`                                                  | `rejects parent summaries whose child is stale by disclosing stale state in plans`                         | Implemented and verified |
| Deterministic smallest-sufficient-context planner and token/cost boundaries                           | `MemoryRetrievalService.plan`                                                                                      | `plans smallest sufficient context, prefers facts and summaries before raw logs, and records exclusions`   | Implemented and verified |
| Evidence-backed Q&A through production coordinator with provenance and stale/contradiction disclosure | `MemoryRetrievalService.answer`, `ProviderExecutionCoordinator`                                                    | `answers through the production coordinator with valid provenance and stale/contradiction disclosure`      | Implemented and verified |
| Deletion, Playground exclusion, cross-owner isolation, Local privacy, and no cloud escalation         | owner/deletion filters; Local provider guard                                                                       | `plans smallest sufficient context...`; `blocks cloud escalation and invalid citations in memory Q&A`      | Implemented and verified |
| Prompt-injection resistance and safe citations                                                        | `<untrusted_memory>` prompt rendering, delimiter blocking, redaction, citation validation                          | `plans smallest sufficient context...`; `blocks cloud escalation and invalid citations in memory Q&A`      | Implemented and verified |

Phase 3-D verification commands: `pnpm --filter @focuslog/desktop exec vitest run --config vitest.config.ts electron/ai/memory-retrieval-service.test.ts electron/database/ai-migration-compat.test.ts`, `pnpm --filter @focuslog/desktop lint`, `pnpm --filter @focuslog/desktop typecheck`.

## Phase 3-E AI memory UI and controls

| Requirement                                                                                                                                             | Implementation                                                                                | Test                                                                                                                            | Status                   |
| ------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| Ownership-checked safe IPC for namespace status, search, facts, graph, staleness, corrections, rebuild/delete, subsystem switches, and safe export      | `electron/ai/memory-control-service.ts`, `electron/main.ts`, `electron/preload.cts`           | `memory-control-service.test.ts` IPC validation/secret scan                                                                     | Implemented and verified |
| Overview shows active namespace, coverage, indexing progress, counts, stale memory, provider/model, storage, diagnostics, and subsystem state           | `AIMemoryControlService.overview`, `AIMemoryPage` overview panel                              | `projects overview, safe diagnostics, counts, and indexing progress without secrets`; `AIMemoryPage.test.ts` overview rendering | Implemented and verified |
| Semantic/hybrid/keyword search exposes metadata, scores, stale state, namespace/model, explanation, and open-source affordance without generation calls | `AIMemoryControlService.search`, `AIMemoryPage` search panel                                  | `returns semantic/hybrid search evidence with namespace disclosure and owner isolation`; `renders search evidence...`           | Implemented and verified |
| Fact inspection, rejection, correction overlay, and re-extract action disclosure distinguish automated extraction from user-curated overlays            | `AIMemoryControlService.factsList`, `rejectFact`, `correctFact`; `AIMemoryPage` fact controls | `supports fact rejection, correction overlays...`; `distinguishes automated facts from user-curated overlays...`                | Implemented and verified |
| Graph inspection, relation evidence, split workflow, and rebuild action without requiring animated graph visualization                                  | `AIMemoryControlService.graph`, `splitGraphEntity`; `AIMemoryPage` graph panel                | `supports fact rejection, correction overlays, graph split...`; `renders graph nodes and edge evidence...`                      | Implemented and verified |
| Derived-memory controls preserve canonical logs and require confirmation for destructive actions                                                        | `AIMemoryControlService.deleteDerived`; `AIMemoryPage` confirmation controls                  | `supports fact rejection... derived deletion without canonical log loss`                                                        | Implemented and verified |
| Renderer UI quality: loading, empty, stale/deleted disclosure, progress, recovery/error, keyboard labels, and secret-free rendering                     | `AIMemoryPage.tsx`, `styles.css`, safe renderer projections                                   | `AIMemoryPage.test.ts` loading/overview/search/fact/graph rendering and secret scans                                            | Implemented and verified |

Phase 3-E verification commands: `pnpm --filter @focuslog/desktop exec vitest run --config vitest.config.ts electron/ai/memory-control-service.test.ts src/renderer/AIMemoryPage.test.ts`, `pnpm --filter @focuslog/desktop lint`, `pnpm --filter @focuslog/desktop typecheck`, `pnpm --filter @focuslog/desktop build`.

## Phase 3-F integrated gate and performance closure

| Requirement                                                                                                                                                                            | Implementation                                                                                                                                   | Test                                                                                                                               | Status                   |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| Deterministic 200,000-log synthetic dataset and reproducible benchmark artifact                                                                                                        | `phase3-performance-harness.ts`, `phase3-performance-harness-runner.ts`, `docs/AI_PERFORMANCE.md`, `artifacts/phase3/phase3-200k-benchmark.json` | Full runner command with 200,000 logs; `phase3-performance-harness.test.ts` default/shape checks                                   | Implemented and verified |
| Chunk generation, vector persistence, retrieval, facts, graph, staleness, deletion, restart, projection, memory, and DB-size measurement                                               | `runPhase3PerformanceHarness` timing sections and artifact schema                                                                                | `phase3-performance-harness.test.ts`; full 200k runner output                                                                      | Implemented and verified |
| Integrated migration/restart validation and repeated initialization safety                                                                                                             | file-backed encrypted DB opened, migrated, closed, reopened, initialized, and counted                                                            | `restartReopenAndInitialize` metric; `ai-migration-compat.test.ts`                                                                 | Implemented and verified |
| Source deletion/change excludes or marks derived chunks, vectors, facts, graph, retrieval evidence, and stale records according to policy                                              | `MemoryRetrievalService.propagateSourceChange`; benchmark targeted deletion/edit checks                                                          | `phase3-performance-harness.test.ts`; `memory-retrieval-service.test.ts`                                                           | Implemented and verified |
| Privacy/security validation: Local boundary, credential redaction, prompt-injection representation, no generation-model semantic search, cross-profile isolation, Playground exclusion | service filters/redaction; safe UI projections; harness security booleans                                                                        | `phase3-performance-harness.test.ts`, `memory-retrieval-service.test.ts`, `memory-control-service.test.ts`, `AIMemoryPage.test.ts` | Implemented and verified |
| Production runtime registers Phase 3 fact/graph durable queue handlers                                                                                                                 | `queue-runtime.ts` registers `FactGraphService.queueHandlers()` with the main worker                                                             | `fact-graph-service.test.ts`; full desktop suite                                                                                   | Implemented and verified |
| Phase 3 code audit, acceptance closure, and hardening separation                                                                                                                       | `AI_PHASE3_ACCEPTANCE.md`, `AI_PHASE3_HARDENING_BACKLOG.md`, `AI_PERFORMANCE.md`; `rg` scans for TODO/FIXME/HACK/skips/mock-only/fake vectors    | audit command recorded in delivery report                                                                                          | Implemented and verified |

Phase 3-F verification commands: `pnpm --filter @focuslog/desktop exec vitest run --config vitest.config.ts electron/ai/phase3-performance-harness.test.ts`, `pnpm --filter @focuslog/desktop build:main`, `node apps/desktop/dist-electron/ai/phase3-performance-harness-runner.js artifacts/phase3/phase3-200k-benchmark.json`, `pnpm --filter @focuslog/desktop lint`, `pnpm --filter @focuslog/desktop typecheck`, `pnpm --filter @focuslog/desktop test`, `pnpm --filter @focuslog/desktop build`.
